/**
 * @param {string} nodeA
 * @param {string} nodeB
 * @param {Graph} graph
 * @return {boolean}
 */
const checkRoute = (nodeA, nodeB, graph) => {
  // Do something here
};

export default checkRoute;
